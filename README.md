# base_bank_transfer
# Dependencies
- Java 17

# Environment (.env file)
```
MYSQL_ROOT_PASSWORD=
SPRING_PROFILES_ACTIVE=
```

# Run database 
```
docker-compose -f docker-compose_mysql.yml up -d --build
```